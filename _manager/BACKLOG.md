# BACKLOG — Memori App

> Повний список фіч за пріоритетом. Статуси: 📋 Backlog | 🔄 In Progress | ✅ Done | ❌ Cancelled

---

## Фідбек User (2026-08-12) — FB-42: недовчені вірші в Бібліотеці — знайдено реальну причину

| # | Фіча | Область | Статус | Пріоритет |
|---|------|---------|--------|-----------|
| FB-42 | **BUG** User показала скріншот: 2 вірші, яких вона ще не довчила, лежать у Бібліотеці замість Прогресу; той самий пункт вона вже піднімала раніше того ж дня — попередній прохід не відтворив і закрив без фіксу | Text / Library / CSS | ✅ Done — 2026-08-12. `registerTextPass`/`TEXT_MASTERY_THRESHOLD=2` (FB-41) підтверджено НЕ зламаним. Реальна причина подвійна: (1) ці 2 записи заархівовані ДО FB-41 за старим правилом, FB-41 свідомо не перераховує старі записи заднім числом; (2) кнопка-ремедіум ↩ "повернути в Прогрес" (`demoteLearnedEntry`) була недосяжна — `.profile-item-learned-row2` (картка Бібліотеки) вилазила за межі екрана на вузьких телефонах через flexbox `min-width:auto` баг (той самий клас, що вже фіксили того ж дня на `.profile-item-actions` для "За наборами", але на іншому селекторі, який тоді не перевірили). Фікс: `min-width:0; width:100%; flex-wrap:wrap` на `.profile-item-learned-row2`. Перевірено `javascript_tool` на 320/375px, світла/темна тема — рядок більше не вилазить за картку, ↩ функціонально працює (переносить запис назад у Прогрес з `passCount=1`). User треба самостійно натиснути ↩ на кожному зі старих 2 віршів — фікс не мігрує їх автоматично. Файли: `style.css` (`?v=`98→100), `index.html`. Деталі — `STATUS.md`, розділ 2026-08-12 "продовження 3" | 🔴 Critical |

---

## Фідбек User (2026-08-11, продовження) — FB-41: "1 прохід = вивчено" не відповідало реальному навчанню

| # | Фіча | Область | Статус | Пріоритет |
|---|------|---------|--------|-----------|
| FB-41 | **BUG/методика** Вірш, який User не довчила (навіть той, що вчила довго), опинявся в Бібліотеці замість Прогресу — бо ОДИН природний прохід тексту вважався "вивчено". User підтвердила: "реально вивчила" = кілька вдалих проходжень. Заразом: "плани не потрібні" (2 таби В роботі/Плани — злити в 1), Бібліотека = фільтр за мовою ТА пошук, Прогрес = переробити на стадії (самий початок/півдороги/трішки і вивчено), креативний текст | Text / Methodology+IA | ✅ Done — 2026-08-11. `TEXT_MASTERY_THRESHOLD=2` (state.js, узгоджено з Words Mode `WT_MASTERY_THRESHOLD`) — `registerTextPass()` рахує повні проходження (allDone), архівує в Learned лише після 2. `showFinal()` (learning.js) — замість прямого `addToLearned()` викликає `registerTextPass()`; при неповному прогресі (1/2) фінальний екран каже "Чудовий прохід! Ще один — і вивчено 🌱" замість "Вивчено!". Прогрес: 2 таби В роботі/Плани злиті в ОДИН список (`renderTextProgress`, app.js) з творчим бейджем стадії — 🌱"Перші кроки" (passCount=0, <50% поточної сесії)/🌿"На півдорозі!" (passCount=0, ≥50% активної сесії)/🌳"Ще трішки — і вивчено!" (passCount=1) — 6 мов. `#progressTabs` прибрано з index.html. Бібліотека (Вивчено): search-інпут (title/text substring, live, той самий патерн що Словник) + вже наявний мовний фільтр. **Ручне виправлення старих даних:** `demoteLearnedEntry(id)` — кнопка ↩ на кожному записі Вивчено повертає його в Прогрес (passCount=1, не з нуля) — старі записи, заархівовані ДО цього фіксу за старим правилом, не перераховуються автоматично (неможливо надійно визначити скільки проходжень насправді було), User сама повертає, якщо не погоджується. Перевірено `javascript_tool`: 1-й прохід → лишається в Прогресі passCount=1; 2-й прохід → переїжджає в Learned; стадії/фільтр/пошук/demote — усі коректні. Файли: `state.js` (`?v=`11→12), `learning.js` (`?v=`13→14), `app.js` (`?v=`109→110), `style.css` (`?v=`95→96) | 🔴 Critical |

---

## Фідбек User (2026-08-11) — 1/5 виправлено, решта потребує підтвердження підходу

> Feedback-дамп + окреме уточнення тим самим повідомленням (мовний фільтр). Кожен пункт звірений з живим кодом перед записом.

| # | Фіча | Область | Статус | Пріоритет |
|---|------|---------|--------|-----------|
| FB-34 | **BUG (критичний, втрата даних)** Вивчений вірш/текст "зникав" з Прогресу; при повторному додаванні одразу опинявся в Бібліотеці ("Вивчено"), хоча ще не вивчений | Text / Bug | ✅ Done — 2026-08-11. Корінна причина: FB-21 (2026-08-08) зробив `addToLearned()` у `showFinal()` (`learning.js`) безумовним — щоб зупинити нав'язливий resume-банер після дострокового виходу, воно заразом стало АРХІВУВАТИ будь-яке дострокове "Завершити тут" як "Вивчено" (з фейковим `blockCount`, часто 1), забираючи текст з "Плани"/"В роботі". Виправлення: `addToLearned()` — лише при `allDone===true` (справжнє природне завершення); `clearState()` лишився безумовним (проблема FB-21 з нав'язливим банером не повертається), при достроковому виході текст лишається в "Плани" (уже туди потрапив автоматично при `goToSetup()`) як незавершений. Файл: `learning.js` (`?v=` 12→13) | 🔴 Critical |
| FB-35 | Редагування вже доданих текстів (Плани) і наборів слів (За наборами) — зараз можна лише перейменувати текст (Плани) чи видалити набір слів, не можна змінити сам вміст. Уточнення User: редагування "прямо в списку" | Text+Words / Edit | ✅ Done — 2026-08-11. **Text (Плани):** `editPlannedText(id)`/`savePlannedTextEdit(id)` (`app.js`) — інлайн, без переходу на екран: картка замінює тіло на `<textarea>` + ✓/✕ (той самий чіп-стильний патерн, що й `editWordChip`), зберігає в `entry.text`, автозаголовок оновлюється лише якщо не було `customTitle`. **Word sets (За наборами):** "✎ Редагувати" відкриває ТУ Ж chip-верифікацію/тему, що вже існує для СТВОРЕННЯ набору (`wordVerifyScreen`→`wordTopicScreen`) — не дублюючи UI, а переиспользуя перевірену логіку (переклад/цикл варіантів/додавання слів). Новий `editingSetId` (`state.js`) сигналізує `saveWordSetAndStart()` (`words.js`) оновити ІСНУЮЧИЙ набір замість створення нового: `masteryScore` зберігається для пар з незмінним word+translation, скидається в 0 для змінених/нових пар. Тема при редагуванні прибирається з автопідбору — показує вже задану User тему. `editingSetId` скидається на старті звичайного флоу створення (`showWordLangScreen`), щоб не "протекти" з покинутого редагування. Перевірено `javascript_tool`: текст — зміна вмісту+перегенерація заголовка; набір слів — chip-екран з прайфілом, тема з прайфілом, `masteryScore` 2/1/(new) → 2/0/0 після редагування одного слова й додавання нового, `editingSetId` коректно null після збереження і при абандоні через звичайний флоу створення | 🔴 High |
| FB-36 | Темна тема: "неоновий" колір робить кнопки погано читабельними (напр. "Завершити" під час тренування слів — зайве сяйво; "На головну" на фіналі — фіолетовий текст на фіолетовому фоні, майже невидимий) | Theme / CSS | ✅ Done — 2026-08-11. Корінна причина знайдена зі скріншотів + `getComputedStyle`, не здогад: `body[data-theme="dark"] button {...}` (`style.css`) мав специфічність 2 елементи (body+button) + 1 атрибут, що в темній темі перебивало БУДЬ-ЯКИЙ одиничний клас (`.btn-ghost`, `.btn-wt-finish`, `.btn-home-topbar` тощо), хоча в світлій темі базовий `button {}` (0 елементів+1 тип) коректно програє тим самим класам. Кожна "тиха" вторинна/іконна кнопка мимоволі отримувала фіолетовий градієнт+сяйво замість свого прозорого/ghost-стилю. Виправлення: `body[data-theme="dark"] button:not([class])` — обмежено лише некласифікованими кнопками (Далі/Почати/Продовжити тощо), той самий принцип, що вже природно працює в світлій темі. Перевірено `getComputedStyle` до/після на throwaway-елементах: `.btn-wt-finish`/`.btn-ghost` тепер `background:transparent`, некласифікована кнопка й далі отримує градієнт — жодної регресії. Файли: `style.css` (`?v=` 93→94) | 🟡 Medium |
| FB-37 | Бібліотека слів: структурувати (пошук/фільтр), бо список зростає і буде важко знайти потрібне. Уточнення User: "і пошук по слову і фільтр за темою/набором" | Words / Library | ✅ Done — 2026-08-11. Словник (`renderWordDictionary`, `words.js`): пошук-інпут (word/translation/topic substring, live) + dropdown фільтр за темою набору (показується лише якщо тем >1) — обидва незалежні, комбінуються з мовним фільтром (FB-38). Фокус/каретка search-інпуту відновлюються вручну після кожного re-render (`updateWdictSearch`), бо innerHTML-патерн екрана знищує стару DOM-ноду на кожен символ. Перевірено `javascript_tool`: пошук "mou"→1 рядок ("mouse"), фільтр теми/мови звужують коректно | 🟡 Medium |
| FB-38 | Розділити Прогрес і Бібліотеку (Text + Words) по мові тексту/набору — український вірш під "українська", англійський текст під "англійська"; фільтр "Всі" за замовчуванням + вибір конкретної мови. Уточнення User: Words теж має ділитись (не лише Text); для Text мова = той самий прапор "Мова тексту" (`#ocrLang`), що User уже обирає на inputScreen — не здогад/евристика | Text+Words / IA | ✅ Done — 2026-08-11. Прапор-чіп фільтр (🌐 Всі + лише мови, що реально є серед записів) доданий у 4 списки: Плани (`renderProfileTab('planned')`), Вивчено (`renderLearnedLibrary`), За наборами (`renderWordProfileList`, вже мав `set.langFrom`/`langTo` — просто додано фільтр), Словник (`renderWordDictionary`, langFrom пробрасывається в кожен рядок). Text: `currentTextLangCode()` (`state.js`) читає `#ocrLang` (реверс-мапа з `tessLang`, audio.js) і зберігає `entry.lang` при `saveToLibrary()`/`addToLearned()` — старі записи без цього поля просто не мають прапора (не ламаються, не показуються під жодним конкретним фільтром окрім 🌐). Спільний хелпер `renderLangFilterBar()` (`state.js`) — один патерн на всі 4 екрани. Перевірено `javascript_tool` на всіх 4 списках: коректна кількість чіпів, коректне звуження при виборі мови, коректний прапор на заголовку елемента | 🟡 Medium |
| FB-39 | Words Mode: набрана фраза/речення мала зараховуватись правильною і без пунктуації (крапка в кінці, тире, кома тощо), і незалежно від регістру першої літери | Words / Bug | ✅ Done — 2026-08-11. Регістр уже нормалізувався (`.toLowerCase()` в `normalizeAnswer`/`normalizeNoParens`, `isAnswerCorrect`), але пунктуація — ні. `.,;:!?…` і лапки-ялинки тепер прибираються повністю; тире/дефіс замінюється на пробіл (не прибирається зовсім), щоб "well-known" не злипався у "wellknown" при наборі через пробіл. Файл: `words.js` (`?v=` 19→20) | 🟡 Medium |
| FB-40 | Words Mode: підказка для багатослівної фрази (напр. німецькою, 4-5 слів) видавала ВСЮ фразу цілком замість частини — User мала додумати решту сама | Words / Bug | ✅ Done — 2026-08-11. `wtShowHint()` (`words.js`) для фрази була ПРОГРЕСИВНОЮ — кожне повторне натискання відкривало ще одне слово, при кількох натисканнях (природний рух, коли одного слова не досить) підказка врешті видавала всю фразу — на відміну від однослівного випадку (перші 2 літери, кнопка одразу деактивується, повторно не натиснути). Вирівняно поведінку: тепер і фраза показує лише ПЕРШЕ слово, кнопка одразу деактивується — той самий принцип, що вже діяв для одного слова. Перевірено `javascript_tool`: 4-слівна німецька фраза → підказка показує лише перше слово, кнопка `disabled`, повторний виклик функції не видає більше; однослівний випадок не зачеплений. Файл: `words.js` (`?v=` 22→23) | 🟡 Medium |

---

## Фідбек User (2026-08-07) — необроблено, чекає пріоритезації

> Один довгий feedback-дамп, розбитий на окремі задачі. Деякі перевірені в коді перед записом (позначено), інші — як є з опису User, потребують уточнення перед делегуванням.

| # | Фіча | Область | Статус | Пріоритет |
|---|------|---------|--------|-----------|
| FB-01 | **BUG** Words Mode "✓✗ Правда чи ні": на екрані завжди показано ОДИН переклад (справжній або дистрактор) — при неправильній відповіді (тобто коли показаний переклад був фальшивим) правильний переклад ніде не з'являється. Треба: показати справжній переклад слова | Words | ✅ Done — 2026-08-07 | 🔴 High |
| FB-02 | Words Mode "🎧 Слухай і познач": після перевірки (все правильно) показати переклад відмічених слів, аналогічно до того, як `audio`-вправа вже показує `word→translation` у фідбеку | Words | ✅ Done — 2026-08-07 | 🟡 Medium |
| FB-03 | Words Mode "🧩 Фраза" (colloc) — редизайн: зараз 1 шаблонне речення з пропуском на слово. Нова механіка: 3 різні короткі фрази/речення з тим самим пропущеним словом одночасно, обрати ОДНЕ слово що підходить до всіх трьох (замість вибору з варіантів під одним реченням) | Words | ✅ Done — 2026-08-07 (морфологія/відмінювання й далі не вирішені, той самий компроміс що й у v1 — задокументовано в коді) | 🟡 Medium |
| FB-04 | Text Mode: іконка "на головну" (дім) під час самого проходження уроку/тексту, не тільки на фінальному екрані | Text | ✅ Done — 2026-08-07 (зберігає прогрес перед виходом, як і "Назад") | 🟡 Medium |
| FB-05 | Text Mode: кнопка "Завершити тут" (достроково вийти з поточного проходження) поруч з іконкою дому під час навчання тексту | Text | ✅ Done — 2026-08-07 (попутно виправлено `showFinal()`: `finalBlocks` показував увесь текст навіть при достроковому виході, тепер — реально пройдені блоки) | 🟡 Medium |
| FB-06 | Text Mode: екран відпочинку (rest break) — зараз лише таймер. Показати весь вивчений на разі шматок тексту + креативно запропонувати вибір: просто відпочити АБО повторити цей шматок протягом паузи — обидва варіанти доступні, без примусу | Text / Methodology | ✅ Done — 2026-08-07 | 🟡 Medium |
| FB-07 | Popup-підказка "як вставити текст" (копіювати/вставити, завантажити файл-скріншот/фото/doc/txt/pdf, сфотографувати). **Перевірено:** сама функція вже існує (`ocrInput` на inputScreen, кнопка "Файл", приймає image/pdf/txt/docx) — це суто задача виявлюваності (discoverability), потрібен інфо-попап за тим самим bottom-sheet патерном, що вже є для налаштувань | Text / UX | ✅ Done — 2026-08-07 | 🟢 Low (функція вже працює) |
| FB-08 | **BUG знайдено:** `updateStats()` (streak/блоки/час на statsBar) викликається ЛИШЕ з `learning.js` (Text Mode). Тренування слів (Words Mode) взагалі не оновлює ці лічильники — тому streak/цифри не ростуть у дні, коли вчать тільки слова. Треба викликати `updateStats()`-еквівалент і з Words Mode | Words / Stats | ✅ Done — 2026-08-07 (лічильник "блоків" для Words = к-сть унікальних слів у сесії, не к-сть вправ) | 🔴 High |
| FB-09 | Words Mode: зведений "словник" — єдиний список ВСІХ слів з УСІХ наборів/тем одночасно: вивчені / на повторення (давно вчили) / ще не вивчені. Зараз прогрес показується лише per-набір на `wordProfileScreen` | Words / Profile | ✅ Done — 2026-08-07, вкладка "Словник" на wordProfileScreen. **Обмеження:** "на повторення" зараз = частковий прогрес (masteryScore), НЕ справжня recency ("давно вчили") — немає часової мітки останнього тренування, треба окрема задача якщо це критично | 🔴 High |
| FB-10 | Профіль: розділити на власне Профіль (фото, ім'я, пошта, країна, місто, вік, поточний настрій) і Прогрес. **Формально скасовує D-009 addendum (2026-08-06)** — тоді "Прогрес" і "Профіль" об'єднали в один пункт, бо здавались дублікатом; тепер User явно підтвердила через 3 раунди макетів + AskUserQuestion, що Бібліотека і Прогрес — різні речі, розділяти | Profile / IA | ✅ Done — 2026-08-07. `profileIdentityScreen`: фото/ім'я/настрій(toggle-пілли)/країна/місто/вік/пошта — усе необов'язково. **Не зроблено:** щотижневий creative-попап настрою (FB-14, окрема майбутня задача — тексти пулу вже написані в макеті) | 🟡 Medium |
| FB-11 | Нав-структура: Профіль / Прогрес / Навчання / Home. Фінальна форма (після 3 раундів макетів): Home — НЕ вкладка, кнопка нагорі (як FB-04); знизу 4 вкладки Навчання/Бібліотека/Прогрес/Профіль | Home / IA | ✅ Done — 2026-08-07, **потім переглянуто того ж дня** (див. FB-13): перший розподіл контенту (Прогрес=статистика, Бібліотека=все інше) виявився неправильним після реального використання — сама структура 4 вкладок лишається, вміст перерозподілено | 🔴 High |
| FB-13 | **Коригування FB-11:** розподіл контенту між Прогрес і Бібліотека був неправильним — статистика (стрік/блоки/час) "ні про що", а "В роботі"/"Плани" загубились у Бібліотеці серед архіву. User: Прогрес = що вчать зараз + що заплановано, Бібліотека = лише завершене (архів) | IA | ✅ Done — 2026-08-07. Прогрес: Text="В роботі"+"Плани" таби, Words="За наборами" (без табів). Бібліотека: Text="Вивчено" (нове `entry.category` text/speech/poem/song, 3 групи-заголовки, "молодець"-хедер), Words="Словник" (без табів). Статистика прибрана з UI зовсім (localStorage-логіка лишена, раптом знадобиться) | 🔴 High |
| FB-14 | Щотижневий creative-попап "як настрій?" — пул фраз що ротуються (тон затверджено в макеті, варіант 3), тригер раз на тиждень. Відкладено з FB-10 — сам toggle настрою вже є на profileIdentityScreen, лише періодичний попап не реалізований | Profile / UX | ✅ Done — 2026-08-07. Тригер: 7 днів від останнього показу (незалежно відповіли чи "Не зараз"), 4 фрази/мову, рандомний вибір при кожному показі | 🟢 Low |
| FB-15 | Words Mode "🔗 Пари" (match): коли слово+переклад збігаються правильно, вони мають з'їжджати разом (напр. вниз, в окрему зону), щоб було видно правильну пару поруч — зараз просто гаснуть/блокуються на своєму місці в 2 розкиданих колонках | Words | ✅ Done — 2026-08-07. Зматчена пара зникає з колонок, з'являється рядком `word→translation` у новій done-області, у порядку завершення (не порядку в наборі) | 🟡 Medium |
| FB-16 | "Частина мови" (`pair.pos`, потрібна для вправи "🧩 Фраза"/FB-12) невидима — ніщо не підказує користувачу, що її треба виставити вручну на кожному слові, тому вправа фактично ніколи не з'являється для реальних наборів. Зробити POS-селектор видимим одразу при введенні/перевірці слів (не схований в режимі "редагувати") | Words / UX | ✅ Done — 2026-08-07. Компактний `—/N/V/Adj` select прямо на чіпі слова (не в edit-режимі); підтверджено на живому тесті, що вправа "Фраза" реально з'являється в черзі після виставлення pos | 🟡 Medium |
| FB-17 | **BUG знайдено (не сьогоднішня зміна):** 24-годинне авто-відновлення сесії (`applyWtSavedProgress`) падає для вправ `match`/`listen` — код шукає `set.pairs.find(p => p.word === ex.pair.word...)`, але ці 2 типи вправ мають `ex.pairs` (масив), не `ex.pair`. Знайдено QA-агентом під час тестування FB-15, не пов'язано з ним — pre-existing | Words / Bug | ✅ Done — 2026-08-09 (FB-26, див. нижче) | 🟡 Medium |
| FB-12 | **BUG знайдено:** вправа "🧩 Фраза" (colloc) після редизайну FB-03 і далі непрацездатна — `COLLOCATION_TEMPLATES` були ПОВНИМИ РЕЧЕННЯМИ, розрахованими на іменник у ролі додатка ("I need ___."). Слово підставлялось без урахування частини мови — для дієслів/прикметників/фраз вправу неможливо було виконати правильно. User: потрібні саме колокації (короткі фрази з прийменниками/дієсловами/прикметниками), а не речення | Words | ✅ Done — 2026-08-07. Рішення обране User через AskUserQuestion (POS-теги, без мережі): новий необов'язковий `pair.pos` (іменник/дієслово/прикметник) на екрані редагування слова (editWordChip), шаблони переписані на короткі фрази ("kind of ___", "want to ___", "very ___"), згруповані по частині мови на кожну з 6 мов. Пари без вказаного pos просто не отримують тип 'colloc' (як і 'sentence' без прикладів) — без помилок для старих наборів. **Реалізовано в ізольованому git worktree** (`.claude/worktrees/colloc-fix`) через паралельну активну сесію CEO в основній робочій директорії | 🔴 High |

---

## Фідбек User (2026-08-08) — ✅ усі 5 пунктів реалізовано

> Один feedback-дамп (5 пунктів), кожен звірений з реальним кодом (grep/read живих файлів, не STATUS.md-заявок) перед записом, потім реалізовано і функціонально перевірено того ж дня. Деталі реалізації/перевірки — `STATUS.md`, розділ 2026-08-08. Попутно за прямою вказівкою User: прибрано щотижневий mood-попап і поле "Настрій" з профілю (D-010 у DECISIONS.md — загальний тон додатку: креативний/теплий, без перебору).

| # | Фіча | Область | Статус | Пріоритет |
|---|------|---------|--------|-----------|
| FB-18 | **BUG** Техпідтримка: одноразова підказка-бульбашка (`.support-hint-bubble`) не піднімалась разом з FAB над bottom-nav; FAB мав лише ~11px запасу без safe-area | Support / Nav | ✅ Done — 2026-08-08. `body.nav-visible .support-hint-bubble` тепер піднімається окремо (bottom:160px vs FAB 96px), усі 3 fixed-елементи (nav/FAB/bubble) отримали `env(safe-area-inset-bottom)`, `.bottom-nav-item` — `white-space:nowrap`. Перевірено через `getAnimations()`-keyframes (screenshot/click недоступні в цій browser-сесії, задокументована обмеженість) | 🟡 Medium |
| FB-19 | **FEATURE** Вибір окремих слів з категорії/набору (або всі з категорії) для тренування + комбінування слів з кількох наборів в одну сесію | Words | ✅ Done — 2026-08-08. Новий `wordSelectScreen` (чекбокси по слову + "Обрати всі" на набір), вхід через "🎯 Обрати слова" на Прогрес→Слова. Комбінувати можна лише в межах ОДНІЄЇ мовної пари (інша пара — toast-відмова). `updateWordMastery()` розширено `__originMap`-логікою: прогрес з комбінованої сесії пишеться точково назад у КОЖЕН реальний набір-джерело, не в один. Перевірено функціонально: 2 слова з 2 наборів → mastery піднявся лише для цих 2 слів, сусідні слова в тих самих наборах не зачеплені | 🟡 Medium |
| FB-20 | Вправа "🧩 Фраза" — прибрано вимогу вручну виставляти частину мови (`pair.pos`), тепер визначається автоматично | Words | ✅ Done — 2026-08-08. `fetchWordPos()` через Google bilingual-dictionary (`dt=bd`, завжди `hl=en` — емпірично саме hl, не tl, визначає мову підпису, тому підпис завжди англійський незалежно від мовної пари). Прибрано весь ручний UI (inline-select на чіпі й у edit-режимі). Перевірено на 4 словах різних частин мови (noun/verb/adj/adverb) — 3 визначились коректно, adverb коректно не отримав pos (не з 3 категорій колокацій) | 🔴 High |
| FB-21 | **BUG** Text Mode: дострокове завершення сесії не чистило `STATE_KEY` → застаріла сесія спливала знову | Text / Bug | ✅ Done — 2026-08-08. `showFinal()` тепер завжди викликає `clearState()`/`addToLearned()` (з реально пройденою к-стю блоків), незалежно від `allDone`. Перевірено функціонально: дострокове `finishLearningHere()` → `STATE_KEY` очищено, resume-banner не показується | 🔴 High |
| FB-22 | Авто-визначення вірш/пісня vs текст/мовлення за римою, 2 категорії замість 4 | Text / Methodology | ✅ Done — 2026-08-08. `detectHasRhyme()` — евристика збігу закінчень рядків (AABB+ABAB, з нормалізацією "німого" e для англійської), викликається в `addToLearned()`. `LEARNED_CATEGORIES` звужено до `text_speech`/`poem_song`, легасі-значення (`text`/`speech`/`poem`/`song`) нормалізуються на льоту без міграції даних. Перевірено на 5 віршах різними мовами (4/5 виявлено) і 4 текстах/промовах (0 хибних спрацювань) | 🟡 Medium |

---

## Фідбек User (2026-08-09) — ✅ усі 4 пункти реалізовано + QA-прохід

> 4-пунктовий feedback + пряма вказівка "запустити тестування, всі баги/недоопрацювання перевірити та виправити". Деталі реалізації/перевірки — `STATUS.md`, розділ 2026-08-09 (FB-23–FB-26).

| # | Фіча | Область | Статус | Пріоритет |
|---|------|---------|--------|-----------|
| FB-23 | **BUG → видалення** Вправа "🧩 Фраза" (колокації) не працює правильно — User попросила прибрати повністю, не лагодити | Words | ✅ Done — 2026-08-09. Вправа, `COLLOCATION_TEMPLATES`, увесь FB-20 pos-detection шар (використовувався лише цією вправою) і всі переклади/CSS видалені. Пряма заміна для FB-03/FB-12/FB-16/FB-20 (нижче) — ці рядки лишені як історія рішень, фіча яку вони описували більше не існує | 🔴 High |
| FB-24 | Кнопка "🎯 Обрати слова" (FB-19) має бути в Бібліотеці, не в Прогресі | Words / IA | ✅ Done — 2026-08-09. Переїхала з `progressContent`/`renderWordProfileList` у `wordDictionaryContent`/`renderWordDictionary`; "Назад" з вибору слів веде в Бібліотеку (`openWordProfile`), не в Прогрес | 🟡 Medium |
| FB-25 | **BUG** Нав-бар все ще перекриває FAB підтримки, попри "перевірений" FB-18 | Support / Nav | ✅ Done — 2026-08-09. Ймовірна причина: FB-18 підняв FAB на ЖОРСТКО зашитий 96px (оцінка висоти бару "на око"), без захисту від росту висоти при масштабуванні шрифту ОС. Замінено на `syncNavFabOffset()` (app.js) — читає реальний `#bottomNav.offsetHeight`, пише `--nav-fab-offset` CSS-змінну динамічно | 🔴 High |
| FB-26 | **BUG (= FB-17 вище)** `applyWtSavedProgress` падає на resume з match/listen у черзі | Words / Bug | ✅ Done — 2026-08-09. Ремап пар для match/listen тепер іде по `ex.pairs` (масив), не `ex.pair`, дзеркалячи вже наявну гілку в `updateWordMastery()` | 🟡 Medium |
| — | **QA-прохід за прямою вказівкою User:** валідація (порожній/короткий текст), потрійний швидкий клік "Почати", resume-банер, повнота перекладів (274/274 ключів у всіх 6 мовах), 2 раніше задокументовані "відомі баги" (`saveToLibrary` textarea, `Date.now()` id-колізія) звірені з живим кодом — обидва вже виправлені попереднім записом, не фабрикація | QA | ✅ Перевірено, нових проблем не знайдено (окрім FB-25/FB-26 вище) | — |

---

## ФАЗА 1 — Polished MVP (тижні 1–2)

Мета: зробити існуючий додаток зручним і завершеним для реального використання.

### UX / UI

| # | Фіча | Агент | Статус | Пріоритет |
|---|------|-------|--------|-----------|
| F-01 | Екран фіналу — привітання, статистика сесії (скільки блоків, скільки хвилин) | Frontend + UX | ✅ Done | 🔴 High |
| F-02 | Кнопка "Ще раз" і "На головну" на фіналі | Frontend | ✅ Done | 🔴 High |
| F-03 | Відображення номера блоку (напр. "Блок 2 з 5") під час NEW | Frontend | ✅ Done | 🔴 High |
| F-10 | Вибір днів: 1 / 2 / 3 / 4–5 / тиждень | Frontend | ✅ Done | 🔴 High |
| F-11 | Налаштування точності: дослівно / близько / вільно | Frontend + UX | ✅ Done | 🔴 High |
| F-12 | "В умі" — інструкційна картка + кнопка готовності | Frontend | ✅ Done | 🔴 High |
| F-13 | "Письмо" — textarea + перевірка з виділенням помилок | Frontend | ✅ Done | 🔴 High |
| F-04 | Анімація появи тексту (плавне fade-in) | Frontend + UX | ✅ Done | 🟡 Medium |
| F-05 | Кнопка "пауза" під час таймера відпочинку | Frontend | ✅ Done | 🟡 Medium |
| F-06 | Звуковий сигнал на початку нового блоку (не лише відпочинок) | Frontend | ✅ Done | 🟢 Low |
| F-07 | Налаштування розміру шрифту (маленький / середній / великий) | Frontend + UX | ✅ Done | 🟡 Medium |
| F-08 | Підтримка emoji у тексті (вірші з ілюстраціями) | Frontend | 📋 | 🟡 Medium |
| UX-01 | Нижня навігація (Навчання/Прогрес/Профіль), окрема для Text/Words mode | Frontend + UX | ✅ Done (2026-08-05) | 🔴 High |
| UX-02 | Playful Premium іконки-плитки (градієнт+тінь) для mode-card Текст/Слова | UX + Frontend | ✅ Done (2026-08-05) | 🟡 Medium |
| UX-03 | Композиція коротких екранів (modeScreen/wordLangScreen/wordTopicScreen): центрування, blob-декор, підрядок, stagger-анімація | UX + Frontend | ✅ Done (2026-08-05) | 🟡 Medium |
| UX-04 | Об'єднати bottom-nav "Прогрес" і "Профіль" в один пункт — зараз обидві кнопки ведуть на той самий екран (`openProfile`/`openWordProfile`), різниться лише scroll-позиція (top vs tabs). User: верх = фото+статистика, низ = вивчено/вчиться зараз/можна додати пізніше (це вже майже 1:1 існуючі profileHero + 3 вкладки В роботі/Вивчено/Плани — фікс, найімовірніше, суто навігаційний: прибрати дубль-кнопку). Спершу відкладено (queued 2026-08-06), того ж дня User попросила зробити одразу — кнопку "Прогрес" видалено з нав-бару, лишився 1 пункт "Профіль" | Frontend | ✅ Done (2026-08-06) | 🟡 Medium |

### Методика

| # | Фіча | Агент | Статус | Пріоритет |
|---|------|-------|--------|-----------|
| M-01 | Налаштування розміру блоку (5 / 10 / 15 слів) замість фіксованих 15 | Methodologist + Frontend | ✅ Done | 🔴 High |
| M-02 | Налаштування часу відпочинку (5 / 10 / 20 / 30 сек) | Frontend | ✅ Done | 🔴 High |
| M-03 | Режим "Підказка" — показати перше слово кожного рядка, а не лише першого | Methodologist + Frontend | ✅ Done — об'єднано з M-04 в один прогресивний хінт на mindCard (рівень 1) | 🟡 Medium |
| M-04 | Режим "Фраза" — показати перші 2-3 слова блоку для підказки | Methodologist + Frontend | ✅ Done — той самий хінт-механізм, рівень 2 | 🟡 Medium |
| M-05 | Вибір кількості повторень перед переходом до нового блоку | Methodologist | 📋 | 🟡 Medium |
| M-06 | Обмежити мовну пару для тренування слів лише мовами, які реально підтримує інтерфейс (uk/en/pl/de/fr/es) | Frontend | ✅ Done — 2026-07-20, WORD_LANGUAGES 18→6 | 🔴 High |
| M-07 | Автовизначення напряму перекладу при введенні слова (щоб тренована сторона завжди лишалась мовою навчання, незалежно якою мовою пари ввели слово) | Frontend | ✅ Done — 2026-07-20, `fetchTranslationSmart` через `sl=auto` | 🔴 High |

### Якість коду

| # | Фіча | Агент | Статус | Пріоритет |
|---|------|-------|--------|-----------|
| Q-01 | Рефакторинг app.js: розбити на модулі (state, ui, audio, learning) | Frontend | 📋 | 🟡 Medium |
| Q-02 | Додати JSDoc коментарі до ключових функцій | Frontend | 📋 | 🟢 Low |
| Q-03 | Обробка помилок (порожній текст, дуже короткий текст) | Frontend + QA | ✅ Done | 🔴 High |

---

### Технічна підтримка

| # | Фіча | Агент | Статус | Пріоритет |
|---|------|-------|--------|-----------|
| S-01 | Кнопка технічної підтримки на кожному екрані (маскот-FAB) — форма звернення (bug/idea) + локальна історія + копіювання в буфер | Frontend + UX | ✅ Done — 2026-07-20, local-only (localStorage `memori_support`), без email/webhook за вибором User | 🔴 High |
| S-02 | Email/webhook-сповіщення про нові звернення (якщо local-only виявиться недостатнім для фідбеку від інших тестувальників) | Frontend | 📋 Відкладено — свідомий вибір User, переглянути якщо тестувальники почнуть повідомляти фідбек не через User | 🟢 Low |

---

## ФАЗА 2 — Persistence & Library (тижні 3–4)

Мета: зберігати прогрес і бібліотеку текстів у localStorage.

| # | Фіча | Агент | Статус | Пріоритет |
|---|------|-------|--------|-----------|
| P-01 | Збереження поточної сесії в localStorage (якщо закрив вкладку — можна продовжити) | Frontend | ✅ Done | 🔴 High |
| P-02 | Бібліотека текстів: зберегти / назвати / відкрити збережений текст | Frontend + UX | ✅ Done — Profile → "Плани" (повний список, Load/rename/delete) | 🔴 High |
| P-03 | Видалення та редагування збережених текстів | Frontend + UX | ✅ Done — Profile → "Плани"/"Вивчено" (delete/rename для обох) | 🟡 Medium |
| P-04 | Історія сесій: дата, текст, результат | Frontend + UX | ✅ Done — Profile → "Вивчено" (дата, к-сть блоків); `addToLearned()` дедуплікує й автопросуває з бібліотеки | 🟡 Medium |
| P-05 | Статистика: скільки текстів вивчено, загальний час | Frontend + UX | ✅ Done — stats bar на inputScreen (🔥 streak / 📖 блоків / ⏱️ час) | 🟡 Medium |
| P-06 | Експорт/імпорт бібліотеки (JSON файл) | Frontend | 📋 | 🟢 Low |

---

## ФАЗА 3 — Gamification & Motivation (тижні 5–6)

Мета: зробити навчання захопливим, особливо для дітей.

| # | Фіча | Агент | Статус | Пріоритет |
|---|------|-------|--------|-----------|
| G-01 | Стрік (серія днів поспіль) + відображення на головному екрані | Frontend + UX | 📋 | 🔴 High |
| G-02 | Досягнення (бейджі): перший текст, 5 текстів, 7 днів поспіль тощо | Methodologist + Frontend + UX | 📋 | 🔴 High |
| G-03 | Анімація зірок / конфеті на фіналі | UX + Frontend | 📋 | 🟡 Medium |
| G-04 | Рівні складності тексту (легкий / середній / важкий) з різними налаштуваннями | Methodologist + Frontend | 📋 | 🟡 Medium |
| G-05 | Персонаж / маскот (різний для дитина/підліток/дорослий) | UX | 📋 | 🟢 Low |
| G-06 | Звукові ефекти для досягнень | Frontend | 📋 | 🟢 Low |

---

## ФАЗА 4 — Parent / Teacher Dashboard (тижні 7–8)

Мета: батьки і вчителі бачать прогрес дитини.

| # | Фіча | Агент | Статус | Пріоритет |
|---|------|-------|--------|-----------|
| D-01 | PIN-захищений режим батьків (статистика дитини) | Frontend + Backend | 📋 | 🔴 High |
| D-02 | Батько може додати текст у бібліотеку дитини | Frontend + UX | 📋 | 🔴 High |
| D-03 | Звіт сесії: скільки разів переглянув, скільки часу витратив | Frontend + Backend | 📋 | 🟡 Medium |
| D-04 | Нагадування (web push або поштою) "Час вчити!" | Backend | 📋 | 🟡 Medium |
| D-05 | Спільна бібліотека класу (вчитель → учні) | Backend | 📋 | 🟢 Low |

---

## ФАЗА 5 — PWA & Offline (тиждень 9)

Мета: встановити як мобільний додаток, працює без інтернету.

| # | Фіча | Агент | Статус | Пріоритет |
|---|------|-------|--------|-----------|
| W-01 | Service Worker (offline підтримка) | Frontend | 📋 | 🔴 High |
| W-02 | Web App Manifest (іконка, назва, кольори) | Frontend + UX | 📋 | 🔴 High |
| W-03 | Prompt "Встановити додаток" на iOS/Android | Frontend | 📋 | 🟡 Medium |
| W-04 | Splash screen при запуску | UX + Frontend | 📋 | 🟢 Low |

---

## ФАЗА 6 — Backend & Accounts (тижні 10–12)

Мета: синхронізація між пристроями, акаунти.

| # | Фіча | Агент | Статус | Пріоритет |
|---|------|-------|--------|-----------|
| B-01 | Реєстрація / логін (email або Google) | Backend + Frontend | 📋 | 🔴 High |
| B-02 | Синхронізація бібліотеки і прогресу між пристроями | Backend + Frontend | 📋 | 🔴 High |
| B-03 | Профіль користувача (ім'я, аватар, роль) | Backend + Frontend + UX | 📋 | 🟡 Medium |
| B-04 | Сімейний акаунт (батько + діти) | Backend + Frontend | 📋 | 🟡 Medium |
| B-05 | API для майбутнього мобільного додатку | Backend | 📋 | 🟢 Low |

---

## IDEAS / Майбутнє (без фази)

- Голосовий ввід тексту (замість друкування)
- AI-допомога: автоматичний поділ на смислові блоки
- Режим "Диктант" — відтворити текст на слух
- Турнір між учнями (хто швидше вивчить)
- Marketplace текстів (вчителі діляться матеріалами)
- Аналітика складності тексту (автоматично)

---

## Технічний борг

| # | Проблема | Агент | Пріоритет |
|---|----------|-------|-----------|
| T-01 | app.js — один монолітний файл, вже >3800 рядків (зростає) | Frontend | 🟡 Medium |
| T-02 | Немає тестів взагалі | QA | 🟡 Medium |
| T-03 | Немає README для розробника | PM | 🟢 Low |

---

## Перед публічним запуском (Launch Readiness)

| # | Фіча | Агент | Статус | Пріоритет |
|---|------|-------|--------|-----------|
| TTS-01 | Перейти з безкоштовного browser speechSynthesis на Google Cloud TTS (Neural2/WaveNet) для природної озвучки слів і текстів | Frontend + PM (бюджет) | 📋 Заплановано, не для тестового етапу | 🔴 High (перед запуском) |
| TTS-02 | Text Mode: TTS-прослуховування блоку (`speakCurrentBlock`) прибрано з методу "Аудіо"/"Голосом" — метод сам лишився (тепер лише запис голосу/ASR через `startVoiceRecord`, доступний для будь-якої мови, без прапорця). Words Mode: audio/dictation обмежено `TTS_VERIFIED_LANGS=[en,de]` (+es не чіпали, лишається доступним) — 2026-08-05, скориговано того ж дня | Frontend | ✅ Done — TTS-крок прибрано з коду виклику (функції лишились, не видалені); ASR-запис без обмежень | 🟡 Medium |

> Рішення User (2026-07-13): зараз — тестовий етап, лишаємось на безкоштовному голосі браузера (з покращеним підбором голосу). Платний провайдер — окреме рішення перед стартом, коли буде визначено бюджет.
> Рішення User (2026-07-22): провайдер обрано наперед — Google Cloud TTS (див. D-006 у DECISIONS.md). Реалізація й далі відкладена до моменту публічного запуску.
> Рішення User (2026-08-05): реальний телефонний тест показав — Text Mode "Аудіо" взагалі не потрібен (озвучка блоку заважає при вивченні віршів/текстів); Words Mode audio/dictation працюють на тестовому пристрої лише для en/de (uk/pl/fr — немає голосу в ОС, es — не перевірено). Обидва звужені прапорцями (TTS-02), не видалені з коду.
> Уточнення User (2026-08-05, того ж дня): попередній прохід сховав ВЕСЬ метод "Аудіо" прапорцем `TEXT_AUDIO_METHOD_ENABLED`, але це перебір — прибрати треба було лише TTS-прослуховування (`speakCurrentBlock`), а запис власного голосу (ASR, `startVoiceRecord`) User хотіла лишити доступним для всіх мов, бо це безкоштовний браузерний API, що не залежить від якості голосу так, як TTS. Прапорець видалено, кнопка методу (перейменована на "Голосом"/"Voice"/...) знову завжди видима — див. DECISIONS.md D-008 addendum і STATUS.md 2026-08-05 (коригування TTS-02).
